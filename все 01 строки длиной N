#По данному числу N выведите все строки длины N из нулей и единиц в лексикографическом порядке.


def func(arr, length):
  if not length:
    print(arr)
    return
  func(arr + '0', length - 1)
  func(arr + '1', length - 1)


n = int(input())
d = ''
func(d,n)
